import { Meta, Story } from '@storybook/blocks';
import theme from '@sds-theme/EECOL/theme.json';
import { TokenList, Code } from '@sds-stories/util';

<Meta title="Docs/Developers/Mixins/Icons"/>

# Icon Mixins

Synergy provides the following mixin to work with icons.

```css
@mixin icon {size};
```

<div className="table margin-top">
    <div>
        <div>Parameter</div>
        <div>Options</div>
    </div>
    <div>
        <div>`size`</div>
        <div>{ Object.entries(theme.semantic.size.icon).map(([k, v]) => <Code k={k} value={v} url="/?path=/docs/synergy-design-system-developer-docs-tokens-icon--docs"/>) }</div>
    </div>
</div>
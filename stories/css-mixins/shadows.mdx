import { Meta, Story } from '@storybook/blocks';
import theme from '../../dist/EECOL/theme.json';
import { TokenList } from '../util';

<Meta title="Synergy Design System/Developer Docs/Mixins/Shadow"/>

# Shadow Mixins

Synergy provides the following mixin to work with shadows.

```css
@mixin shadow {size};
```

<div className="table margin-top">
    <div>
        <div>Parameter</div>
        <div>Options</div>
    </div>
    <div>
        <div>`size`</div>
        <div>`sm` `md` `lg` `xl`</div>
    </div>
</div>